@page
@model FacturasModel

@{
    ViewData["Title"] = "Gestión de Facturas";
}

<div class="container-fluid py-4">
    <form method="POST" enctype="multipart/form-data">
        @Html.AntiForgeryToken()

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="text-white fw-bold mb-0">🧾 Panel de Gestión de Facturas</h2>
                <p class="text-white-50 small">Administra los registros de facturas del sistema.</p>
            </div>
            <div class="btn-group gap-2 shadow-sm">
                <button type="submit" asp-page-handler="BtNuevo" class="btn btn-primary px-4 rounded-pill"
                        disabled="@(Model.Factura != null)">
                    ➕ Nuevo
                </button>
                <button type="submit" asp-page-handler="BtRefrescar" class="btn btn-outline-light px-3 rounded-pill">
                    🔄 Sincronizar
                </button>
            </div>
        </div>

        @if (Model.Factura != null)
        {
            <div class="card card-crud mb-5">
                <div class="card-header py-3 border-bottom d-flex justify-content-between align-items-center">
                    <h5 class="mb-0 text-primary fw-bold">
                        @(Model.Factura.Id == 0 ? "✨ Registrar Nuevo Factura" : "📝 Modificar Factura ID: " + Model.Factura.Id)
                    </h5>
                    <button type="submit" asp-page-handler="BtGuardar" class="btn btn-success px-4 rounded-pill">
                        💾 Guardar Cambios
                    </button>
                </div>
                <div class="card-body p-4">
                    <div class="row g-3">
                        <input type="hidden" asp-for="@Model.Factura!.Id" />

                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Num Factura</label>
                            <input class="form-control" asp-for="@Model.Factura!.NumFactura" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Fecha Emision</label>
                            <input type="date" class="form-control" asp-for="@Model.Factura!.FechaEmision" />
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Total</label>
                            <input type="number" class="form-control" placeholder="0" asp-for="@Model.Factura!.Total" />
                        </div>
                        <div class="col-md-4 d-flex align-items-center mt-2">
                            <div class="form-check form-switch ps-5">
                                <input class="form-check-input scale-125" type="checkbox" asp-for="@Model.Factura!.EstadoPago" />
                                <label class="form-check-label fw-semibold text-muted ms-2">EstadoPago Activo</label>
                            </div>
                        </div>
                        <div class="col-12 mt-4">
                            <h6 class="text-uppercase text-muted small border-bottom pb-2">Relaciones</h6>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Tipo de Pago</label>
                            @if (Model.ListaTipoPagos != null)
                            {
                                <select class="form-select" asp-for="@Model.Factura!.TipoPago"
                                        asp-items="@(new SelectList(Model.ListaTipoPagos, "Id", "Nombre"))">
                                    <option value="">-- Seleccione Tipo de Pago --</option>
                                </select>
                            }
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Oferta</label>
                            @if (Model.ListaOfertas != null)
                            {
                                <select class="form-select" asp-for="@Model.Factura!.Oferta"
                                        asp-items="@(new SelectList(Model.ListaOfertas, "Id", "Nombre"))">
                                    <option value="">-- Seleccione una Oferta --</option>
                                </select>
                            }
                        </div>
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Evento</label>
                            @if (Model.ListaEventos != null)
                            {
                                <select class="form-select" asp-for="@Model.Factura!.Evento"
                                        asp-items="@(new SelectList(Model.ListaEventos, "Id", "Nombre"))">
                                    <option value="">-- Seleccione un Evento --</option>
                                </select>
                            }
                        </div>

                    </div>
                </div>
            </div>
        }

        @if (Model.Lista != null && Model.Lista.Count > 0)
        {
            <div class="card card-crud">
                <div class="card-header py-3">
                    <h5 class="mb-0 text-white fw-bold">📋 Registro de Facturas</h5>
                </div>
                <div class="table-responsive table-responsive-custom">
                    <table class="tb_data mb-0">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Num Factura</th>
                                <th>Fecha Emision</th>
                                <th>Total</th>
                                <th>Estado Pago</th>
                                <th>Tipo Pago</th>
                                <th>Oferta</th>
                                <th>Evento</th>
                                <th class="text-center">Operaciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var elemento in Model.Lista)
                            {
                                <tr>
                                    <td><strong>#@elemento.Id</strong></td>
                                    <td>@elemento.NumFactura</td>
                                    <td>@elemento.FechaEmision.ToString("dd/MM/yyyy")</td>
                                    <td>@elemento.Total</td>
                                    <td>@if (elemento.EstadoPago)
                                {
                                    <span class="badge bg-success-subtle text-success px-3 py-1 rounded-pill">Activo</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger-subtle text-danger px-3 py-1 rounded-pill">Inactivo</span>
                                }</td>
                                    <td>@elemento.TipoPago</td>
                                    <td>@elemento.Oferta</td>
                                    <td>@elemento.Evento</td>

                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm shadow-sm" role="group">
                                            <button type="submit" asp-page-handler="BtModificar"
                                                    asp-route-data="@elemento.Id"
                                                    class="btn btn-warning text-dark fw-semibold">
                                                ✏️ Editar
                                            </button>
                                            <button type="submit" asp-page-handler="BtBorrarVal"
                                                    asp-route-data="@elemento.Id"
                                                    class="btn btn-danger">
                                                🗑️ Borrar
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }

        @if (ViewData.ContainsKey("Mensaje"))
        {
            @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
        }

        @if (Model.Factura != null && Model.Borrando)
        {
            <div class="popup">
                <div class="pp_inner text-center">
                    <div class="text-danger mb-3" style="font-size: 3rem;">⚠️</div>
                    <h4 class="fw-bold text-white mb-2">¿Confirmar Eliminación?</h4>
                    <p class="text-muted mb-4">Está a punto de borrar permanentemente: <br>
                        <strong class="text-white">"@Model.Factura!.NumFactura"</strong>
                    </p>
                    <div class="d-flex justify-content-center gap-3">
                        <button type="submit" asp-page-handler="BtCerrar"
                                class="btn btn-outline-light px-4 rounded-pill">Cancelar</button>
                        <button type="submit" asp-page-handler="BtBorrar"
                                class="btn btn-danger px-4 rounded-pill">Sí, Eliminar</button>
                    </div>
                </div>
            </div>
        }

    </form>
</div>
